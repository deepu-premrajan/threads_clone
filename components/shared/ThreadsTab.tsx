interface Props {
  currentUserId: string;
  accountId: string;
  accountType: string;
}

const ThreadsTab = async ({ currentUserId, accountId, accountType }: Props) => {
  // function to fetch profile threads

  return <section>ThreadsTab</section>;
};

export default ThreadsTab;
